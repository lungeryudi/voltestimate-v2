[build]
builder = "nixpacks"

[deploy]
startCommand = "cp public/test.html dist/test.html 2>/dev/null || true && npx serve dist -l 3000 --single"
restartPolicyType = "on_failure"

[env]
NODE_ENV = "production"
